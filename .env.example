# Copy to .env and set values locally or in the host (Vercel) dashboard.

# Search Console HTML tag verification (optional)
# PUBLIC_GOOGLE_SITE_VERIFICATION=

# AdSense publisher ID, e.g. ca-pub-xxxxxxxxxxxxxxxx — set only when applying / serving ads
# PUBLIC_ADSENSE_PUBLISHER_ID=
